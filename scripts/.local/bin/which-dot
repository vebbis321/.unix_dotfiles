#!/usr/bin/env bash
if [[ "$OSTYPE" == "linux-gnu"* ]]; then
    source ~/.local/bin/tmux-sessionizer ~/.linux_dotfiles
elif [[ "$OSTYPE" == "darwin"* ]]; then
    source ~/.local/bin/tmux-sessionizer ~/.mac_dotfiles
fi
